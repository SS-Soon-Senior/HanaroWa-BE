package com.ss.hanarowa.domain.lesson.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface RoomTimeRepository extends JpaRepository<RoomTimeRepository, Long> {
}